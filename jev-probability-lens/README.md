# JEV Probability Lens

Standalone Vercel project for the TypeSafe Jev probability demo.

- `/` — Probability Lens UI
- `/api/probability` — server-side Jev proxy
- `/api/health` — readiness check

The TypeSafe API key is stored only in Vercel Environment Variables as `TYPESAFE_API_KEY`.
